Some shell scripts for pababit syslog analysis.

### 2015-12-09 Re-organization
Panabit syslog is the data stored in MongoDB. And the MapReduce functionality(it may be out of date) is provided by MongoDB itself.

### 2012-05-11
SyslogMapReduce.js is the script which runs in the JavaScript shell used by MongoDB.

MongoDBTest.sh is the bash script to collect the performance of the machines in the cluster.

CollStats.js is used by MongoDBTest.sh